import { Reducer } from "redux";
import { AnimationActionTypes, AnimationState } from "./types";

const initialState: AnimationState = null;

export const reducer: Reducer<AnimationState, AnimationActionTypes> = (
    state,
    action
) => {
    return state;
};
